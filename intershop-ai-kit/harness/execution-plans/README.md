# Execution Plans

Create one execution plan per actual ticket. These files contain task-specific discoveries and decisions; they do not replace reusable implementation guides.

Suggested name: `<ticket>-<short-feature-name>.md`.

Do not store credentials, tokens, personal data, or environment secrets in a plan.
